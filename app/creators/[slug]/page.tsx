import Link from "next/link";
import { ArrowLeft, ArrowUpRight, BadgeCheck, Instagram } from "lucide-react";
import { Footer, Navbar, Reveal } from "../../../components/SiteUI";
import site from "../../../content/site.json";

export function generateStaticParams(){return site.creators.map(c=>({slug:c.slug}))}
export default function CreatorProfile({params}:{params:{slug:string}}){const c=site.creators.find(x=>x.slug===params.slug)||site.creators[0];return <main><Navbar/><section className="bg-[#0A0A0A] pt-40 pb-16 text-white sm:pt-48"><div className="mx-auto max-w-[1440px] px-5 sm:px-8"><Link href="/creators" className="inline-flex items-center gap-2 text-xs font-bold uppercase tracking-[.14em] text-white/45 hover:text-white"><ArrowLeft size={14}/> Marketplace</Link><div className="mt-12 grid gap-10 lg:grid-cols-[.55fr_1fr] lg:items-end"><Reveal><div className="overflow-hidden rounded-[30px] border border-white/10"><img src={c.image} alt={c.name} className="aspect-[4/5] w-full object-cover"/></div></Reveal><Reveal delay={.08}><div><div className="flex items-center gap-3"><p className="eyebrow text-[#BB7C1D]">{c.niche} · {c.city}, India</p>{c.verified&&<BadgeCheck className="text-[#BB7C1D]" size={17}/>}</div><h1 className="mt-5 text-5xl font-bold tracking-[-.06em] sm:text-8xl">{c.name}</h1><p className="mt-3 text-white/45">{c.handle}</p><div className="mt-10 grid grid-cols-3 border-y border-white/10 py-5"><div><p className="text-2xl font-bold">{c.followers}</p><p className="mt-1 text-[10px] uppercase tracking-widest text-white/35">Followers</p></div><div><p className="text-2xl font-bold">{c.engagement}</p><p className="mt-1 text-[10px] uppercase tracking-widest text-white/35">Engagement</p></div><div><p className="text-2xl font-bold">{c.rate}</p><p className="mt-1 text-[10px] uppercase tracking-widest text-white/35">Starting</p></div></div><div className="mt-8 flex flex-wrap gap-3"><Link href="/contact" className="inline-flex items-center gap-3 rounded-full bg-[#BB7C1D] px-5 py-3 text-[11px] font-bold uppercase tracking-[.14em]">Request this creator <ArrowUpRight size={15}/></Link><a href="#" className="inline-flex items-center gap-2 rounded-full border border-white/15 px-5 py-3 text-[11px] font-bold uppercase tracking-[.14em] text-white/65"><Instagram size={15}/> Instagram</a></div></div></Reveal></div></div></section><section className="bg-[#f7f7f5] py-24"><div className="mx-auto grid max-w-[1440px] gap-12 px-5 sm:px-8 lg:grid-cols-[.8fr_1.2fr]"><Reveal><p className="eyebrow text-[#BB7C1D]">Media kit snapshot</p><h2 className="mt-5 text-4xl font-bold tracking-[-.05em] sm:text-6xl">A creator who fits the <span className="text-[#BB7C1D]">brief.</span></h2></Reveal><Reveal delay={.08}><div className="grid gap-3 sm:grid-cols-2">{['Instagram Reels','Story packages','UGC content','Product integrations','Event coverage','Long-term ambassador'].map(x=><div key={x} className="rounded-2xl bg-white p-5 text-sm font-semibold">{x}</div>)}</div></Reveal></div></section><Footer/></main>}
