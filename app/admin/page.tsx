"use client";
import { useState } from "react";
import { Save, ShieldCheck } from "lucide-react";
import site from "../../content/site.json";

export default function AdminPage(){const [data,setData]=useState<any>(site);const [password,setPassword]=useState("");const [status,setStatus]=useState("");const [advanced,setAdvanced]=useState(JSON.stringify({creators:site.creators,work:site.work},null,2));const updateHero=(key:string,value:string)=>setData((d:any)=>({...d,hero:{...d.hero,[key]:value}}));const updateStat=(i:number,key:string,value:string)=>setData((d:any)=>({...d,stats:d.stats.map((s:any,n:number)=>n===i?{...s,[key]:value}:s)}));const publish=async()=>{setStatus("Publishing…");let next=data;try{const extra=JSON.parse(advanced);next={...data,creators:extra.creators,work:extra.work};}catch{setStatus("Advanced JSON is not valid.");return}const res=await fetch('/api/admin/content',{method:'POST',headers:{'Content-Type':'application/json','x-admin-password':password},body:JSON.stringify(next)});const out=await res.json();setStatus(out.message||out.error||'Done');};return <main className="min-h-screen bg-[#f0f0ed] text-[#2B2B2B]"><div className="mx-auto max-w-[1400px] px-5 py-8 sm:px-8"><div className="flex flex-col justify-between gap-5 rounded-[24px] bg-[#0A0A0A] p-6 text-white sm:flex-row sm:items-center sm:p-8"><div><p className="text-[10px] font-bold uppercase tracking-[.18em] text-[#BB7C1D]">CreatorFox CMS</p><h1 className="mt-2 text-3xl font-bold tracking-[-.04em]">Content dashboard</h1><p className="mt-2 text-sm text-white/45">Edit the site without touching the frontend.</p></div><div className="flex items-center gap-3 rounded-full border border-white/10 px-4 py-3 text-[10px] font-bold uppercase tracking-widest text-white/50"><ShieldCheck size={15} className="text-[#BB7C1D]"/> GitHub-backed publishing</div></div><div className="mt-5 grid gap-5 lg:grid-cols-[1fr_.7fr]"><section className="space-y-5"><div className="rounded-[24px] bg-white p-6 sm:p-8"><p className="text-[10px] font-bold uppercase tracking-[.16em] text-[#BB7C1D]">Homepage hero</p><div className="mt-6 grid gap-4"><label className="text-xs font-bold">Eyebrow<input className="field mt-2" value={data.hero.eyebrow} onChange={e=>updateHero('eyebrow',e.target.value)}/></label><label className="text-xs font-bold">Headline<input className="field mt-2" value={data.hero.title} onChange={e=>updateHero('title',e.target.value)}/></label><label className="text-xs font-bold">Description<textarea className="field mt-2 resize-none" rows={4} value={data.hero.description} onChange={e=>updateHero('description',e.target.value)}/></label><div className="grid gap-4 sm:grid-cols-2"><label className="text-xs font-bold">Primary CTA<input className="field mt-2" value={data.hero.primaryCta} onChange={e=>updateHero('primaryCta',e.target.value)}/></label><label className="text-xs font-bold">Secondary CTA<input className="field mt-2" value={data.hero.secondaryCta} onChange={e=>updateHero('secondaryCta',e.target.value)}/></label></div></div></div><div className="rounded-[24px] bg-white p-6 sm:p-8"><p className="text-[10px] font-bold uppercase tracking-[.16em] text-[#BB7C1D]">Homepage stats</p><div className="mt-6 grid gap-4 sm:grid-cols-2">{data.stats.map((s:any,i:number)=><div key={i} className="rounded-2xl bg-[#f0f0ed] p-4"><input className="field bg-white" value={s.value} onChange={e=>updateStat(i,'value',e.target.value)}/><input className="field mt-2 bg-white" value={s.label} onChange={e=>updateStat(i,'label',e.target.value)}/></div>)}</div></div><div className="rounded-[24px] bg-white p-6 sm:p-8"><p className="text-[10px] font-bold uppercase tracking-[.16em] text-[#BB7C1D]">Advanced content</p><p className="mt-2 text-xs text-black/45">Edit creator and case-study JSON when you need full control.</p><textarea className="field mt-5 min-h-[420px] resize-y font-mono text-xs" value={advanced} onChange={e=>setAdvanced(e.target.value)}/></div></section><aside className="lg:sticky lg:top-5 lg:h-fit"><div className="rounded-[24px] bg-[#2B2B2B] p-6 text-white sm:p-8"><p className="text-[10px] font-bold uppercase tracking-[.16em] text-[#BB7C1D]">Publish</p><h2 className="mt-4 text-3xl font-bold tracking-[-.04em]">Make the changes live.</h2><p className="mt-4 text-sm leading-6 text-white/45">Publishing commits the content file to GitHub. Your connected Vercel project then rebuilds the website.</p><label className="mt-8 block text-xs font-bold text-white/70">Admin password<input type="password" className="field mt-2 bg-white/10 text-white placeholder:text-white/25" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Configured in Vercel"/></label><button onClick={publish} className="mt-4 flex w-full items-center justify-center gap-3 rounded-full bg-[#BB7C1D] px-5 py-4 text-[11px] font-bold uppercase tracking-[.14em] text-white"><Save size={15}/> Publish changes</button>{status&&<p className="mt-4 rounded-xl border border-white/10 p-3 text-xs leading-5 text-white/65">{status}</p>}<div className="mt-8 border-t border-white/10 pt-5 text-[10px] leading-5 text-white/30">Required Vercel environment variables: ADMIN_PASSWORD, GITHUB_TOKEN, optional GITHUB_REPO.</div></div></aside></div></div></main>}
