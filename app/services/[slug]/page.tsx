import Link from "next/link";
import { ArrowLeft, ArrowUpRight, CheckCircle2 } from "lucide-react";
import { Footer, Navbar, Reveal } from "../../../components/SiteUI";
import site from "../../../content/site.json";

export function generateStaticParams(){return site.services.map(s=>({slug:s.slug}))}

export default function ServicePage({params}:{params:{slug:string}}){const service=site.services.find(s=>s.slug===params.slug)||site.services[0];return <main><Navbar/><section className="bg-[#0A0A0A] pt-40 pb-24 text-white sm:pt-48 sm:pb-32"><div className="mx-auto max-w-[1440px] px-5 sm:px-8"><Reveal><Link href="/services" className="inline-flex items-center gap-2 text-xs font-bold uppercase tracking-[.14em] text-white/45 hover:text-white"><ArrowLeft size={14}/> All services</Link><p className="eyebrow mt-14 text-[#BB7C1D]">{service.title}</p><h1 className="display mt-5 max-w-5xl">{service.title}<span className="text-[#BB7C1D]">.</span></h1><p className="mt-8 max-w-2xl text-base leading-7 text-white/50">{service.short}</p></Reveal></div></section><section className="bg-[#f7f7f5] py-24 sm:py-32"><div className="mx-auto grid max-w-[1440px] gap-14 px-5 sm:px-8 lg:grid-cols-[.75fr_1.25fr]"><Reveal><p className="eyebrow text-[#BB7C1D]">How we approach it</p><h2 className="mt-6 text-4xl font-bold leading-tight tracking-[-.05em] sm:text-6xl">Designed around<br/>the <span className="text-[#BB7C1D]">outcome.</span></h2></Reveal><Reveal delay={.08}><p className="text-lg leading-8 text-black/60">{service.short} {service.slug==='website-development'?'We combine UX, content, performance and technical foundations so the site becomes a growth asset rather than a brochure.':service.slug==='creator-marketing'?'We match creator voice, audience quality, content fit and campaign economics before outreach begins.':'We build the strategy, creative system, execution plan and measurement layer around the business problem — then keep improving it.'}</p><div className="mt-12 grid gap-3 sm:grid-cols-2">{['Strategy & discovery','Creative direction','Execution system','Measurement & optimization'].map(x=><div key={x} className="flex items-center gap-3 rounded-2xl bg-white p-5"><CheckCircle2 className="text-[#BB7C1D]" size={19}/><span className="text-sm font-semibold">{x}</span></div>)}</div><Link href="/contact" className="mt-10 inline-flex items-center gap-3 rounded-full bg-[#2B2B2B] px-5 py-3 text-[11px] font-bold uppercase tracking-[.14em] text-white">Discuss this service <ArrowUpRight size={15}/></Link></Reveal></div></section><Footer/></main>}
