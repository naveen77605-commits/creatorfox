"use client";
import {useEffect,useRef} from "react";
export default function FoxCanvas(){const ref=useRef<HTMLCanvasElement>(null);useEffect(()=>{const c=ref.current;if(!c)return;const x=c.getContext('2d')!;let raf=0;const p=Array.from({length:90},(_,i)=>({x:Math.random(),y:Math.random(),r:Math.random()*1.8+.4,a:Math.random()*.55+.15,s:Math.random()*.0007+.0002}));const resize=()=>{c.width=innerWidth*devicePixelRatio;c.height=innerHeight*devicePixelRatio;x.setTransform(devicePixelRatio,0,0,devicePixelRatio,0,0)};resize();addEventListener('resize',resize);let mx=innerWidth/2,my=innerHeight/2;const move=(e:MouseEvent)=>{mx=e.clientX;my=e.clientY};addEventListener('mousemove',move);const draw=()=>{x.clearRect(0,0,innerWidth,innerHeight);p.forEach(q=>{q.y-=q.s;if(q.y<0)q.y=1;const px=q.x*innerWidth,py=q.y*innerHeight;const dx=(mx-innerWidth/2)*.018,dy=(my-innerHeight/2)*.018;x.beginPath();x.arc(px+dx*(1-q.y),py+dy*(1-q.x),q.r,0,Math.PI*2);x.fillStyle=`rgba(187,124,29,${q.a})`;x.fill()});raf=requestAnimationFrame(draw)};draw();return()=>{cancelAnimationFrame(raf);removeEventListener('resize',resize);removeEventListener('mousemove',move)}},[]);return <canvas ref={ref} className="pointer-events-none absolute inset-0 h-full w-full"/>}
