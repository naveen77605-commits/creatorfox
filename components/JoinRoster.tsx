"use client";import {useState} from "react";import {ArrowUpRight,X} from "lucide-react";export default function JoinRoster(){const[open,setOpen]=useState(false);const[sent,setSent]=useState(false);return <><button onClick={()=>setOpen(true)} className="flex items-center gap-3 rounded-full bg-ink px-6 py-4 text-xs font-bold uppercase tracking-widest text-paper">Join the roster <ArrowUpRight size={15}/></button>{open&&<div className="fixed inset-0 z-50 grid place-items-center bg-ink/75 p-5"><div className="relative w-full max-w-lg rounded-2xl bg-paper p-7 text-charcoal"><button onClick={()=>setOpen(false)} className="absolute right-5 top-5"><X/></button>{sent?<><h3 className="text-3xl font-bold">Application received.</h3><p className="mt-3 text-sm text-charcoal/55">We’ll review your profile and contact you if there’s a fit.</p></>:<><p className="kicker text-gold">CreatorFox roster</p><h3 className="mt-3 text-3xl font-bold">Let’s work together.</h3><form onSubmit={e=>{e.preventDefault();setSent(true)}} className="mt-7 grid gap-3">{['Name','Instagram handle','Niche','Followers','Engagement %','Starting rate','Reel links'].map(x=><input key={x} required placeholder={x} className="rounded-xl border border-charcoal/15 bg-white px-4 py-3 text-sm outline-none focus:border-gold"/>)}<button className="mt-2 rounded-full bg-ink py-3 text-xs font-bold uppercase tracking-widest text-paper">Submit profile</button></form></>}</div></div>}</>}
