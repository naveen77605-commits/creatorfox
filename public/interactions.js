const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;
document.querySelectorAll('[data-explorer]').forEach(root=>{const tabs=[...root.querySelectorAll('[role=tab]')],panels=[...root.querySelectorAll('[role=tabpanel]')];function choose(i){tabs.forEach((t,n)=>{t.setAttribute('aria-selected',String(n===i));t.tabIndex=n===i?0:-1;panels[n].hidden=n!==i})}tabs.forEach((t,i)=>{t.addEventListener('click',()=>choose(i));t.addEventListener('keydown',e=>{let n=i;if(e.key==='ArrowDown'||e.key==='ArrowRight')n=(i+1)%tabs.length;else if(e.key==='ArrowUp'||e.key==='ArrowLeft')n=(i+tabs.length-1)%tabs.length;else if(e.key==='Home')n=0;else if(e.key==='End')n=tabs.length-1;else return;e.preventDefault();choose(n);tabs[n].focus()})})});
document.querySelectorAll('[data-engagement]').forEach(root=>root.querySelectorAll('[data-mode]').forEach(button=>button.addEventListener('click',()=>{root.querySelectorAll('[data-mode]').forEach(b=>b.setAttribute('aria-pressed',String(b===button)));root.querySelectorAll('[data-mode-panel]').forEach(p=>p.hidden=p.dataset.modePanel!==button.dataset.mode)})));
const goal=document.querySelector('#goal-choice');if(goal)goal.addEventListener('change',()=>{const groups={brand:['brand-kit','3d-website-development'],growth:['content-marketing','performance-marketing','influencer-marketing','aeo-geo'],ai:['ai-workshop','digital-ai-consulting','automation-ai-agents']};document.querySelectorAll('.service-card').forEach(c=>c.hidden=goal.value!=='all'&&!groups[goal.value].some(s=>c.getAttribute('href').endsWith('/'+s)));document.querySelector('#goal-advice').textContent=({all:'Explore the full range. Start with one priority or combine services around a shared goal.',brand:'Start with how your business is positioned, presented and experienced online.',growth:'Connect your content, discovery and acquisition to the audience you want to reach.',ai:'Build team confidence and identify practical opportunities before adding more tools.'})[goal.value]});
document.querySelectorAll('[data-filter]').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('[data-filter]').forEach(x=>x.setAttribute('aria-pressed',String(x===b)));document.querySelectorAll('.industry-cards .card').forEach(c=>c.hidden=b.dataset.filter!=='all'&&!c.querySelector('h3').textContent.toLowerCase().includes(b.dataset.filter.toLowerCase()))}));
if(!reduced&&matchMedia('(pointer:fine)').matches){document.querySelectorAll('[data-tilt]').forEach(el=>{el.addEventListener('pointermove',e=>{const r=el.getBoundingClientRect();el.style.setProperty('--tx',((e.clientX-r.left)/r.width-.5)*8+'deg');el.style.setProperty('--ty',-((e.clientY-r.top)/r.height-.5)*6+'deg')});el.addEventListener('pointerleave',()=>{el.style.setProperty('--tx','0deg');el.style.setProperty('--ty','0deg')})});document.querySelectorAll('.service-card').forEach(el=>el.addEventListener('pointermove',e=>{const r=el.getBoundingClientRect();el.style.setProperty('--mx',e.clientX-r.left+'px');el.style.setProperty('--my',e.clientY-r.top+'px')}))}
